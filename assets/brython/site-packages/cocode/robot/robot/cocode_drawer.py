import json
from .json_drawer import JsonDrawer


class CocodeDrawer(JsonDrawer):
    def __init__(self):
        super().__init__()
        from browser import window
        window.sendMsg('robot-init')

    def print(self, content):
        from browser import window
        window.sendMsg('robot-draw', content)
